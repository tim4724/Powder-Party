{
  "name": "powder-party",
  "version": "0.1.0",
  "description": "Browser-based multiplayer downhill ski racing with phone tilt + swipe controllers",
  "main": "server/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node scripts/build.js && node server/index.js",
    "dev": "USE_BUNDLES=0 node --watch server/index.js",
    "test": "node --test tests/*.test.js partyplug/tests/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "esbuild": "^0.28.1",
    "three": "^0.184.0"
  }
}
